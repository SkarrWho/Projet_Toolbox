services:
  toolbox:
    build: .
    container_name: toolbox
    stdin_open: true      
    tty: true  
    ports:
      - "5000:5000"
      - "4444:4444"
    networks:
      - network

networks:
  network:
    driver: bridge
